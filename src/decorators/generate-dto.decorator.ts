/**
 * Class decorator untuk menandai sebuah entity agar DTO-nya dibuat secara otomatis.
 */
export function GenerateDto(): ClassDecorator {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  return () => {};
}
